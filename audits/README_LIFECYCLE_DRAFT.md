# Audit Lifecycle Organization — Staging Note

This temporary staging note exists only while the audit files are being reorganized in the current session. It will be removed after `audits/README.md` is updated and the lifecycle folders contain the moved audit records.
