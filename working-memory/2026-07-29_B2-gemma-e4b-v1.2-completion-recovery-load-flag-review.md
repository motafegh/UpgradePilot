# B2 Gemma E4B v1.2 Completion-Recovery Load-Flag Review

**Date:** 2026-07-29  
**Operation:** Independently review the first v1.2 completion-recovery attempt and select the smallest corrected continuation  
**Reviewed result:** [`2026-07-29_B2-gemma-e4b-v1.2-completion-recovery-result.md`](2026-07-29_B2-gemma-e4b-v1.2-completion-recovery-result.md)  
**Raw evidence:** [`evidence/2026-07-29-gemma-e4b-v1.2-completion-recovery/`](evidence/2026-07-29-gemma-e4b-v1.2-completion-recovery/)  
**Result classification:** Evidence accepted as a runner-load failure; no model inference occurred; corrected one-response rerun selected; no model or product adoption

## Audit conclusion

The evidence bundle is complete enough to determine the failure precisely.

Observed:

```text
preflight: passed
pre-load GPU used: 1383 MiB
pre-load GPU free: 6636 MiB
LM Studio loaded models before run: none
load command exit: 1
model inference: not run
unload/cleanup exit: 0
post-cleanup loaded models: none
product tests: 64 passed
manifest verification: 63/63 passed
```

The required resource baseline was met. The failure happened before model loading and before any semantic request.

## Exact root cause

LM Studio rejected this runner argument:

```text
--no-speculative-draft-simple
```

The preserved CLI error was:

```text
error: unknown option '--no-speculative-draft-simple'
(Did you mean --speculative-draft-simple?)
```

The earlier accepted Gemma load command already disabled MTP speculative drafting with:

```text
--no-speculative-draft-mtp
```

and the observed loaded configuration reported both simple and model speculative drafting as false without requiring the unsupported negative-simple flag. The completion-recovery runner introduced an unnecessary extra flag.

## Causal classification

```text
unsupported local CLI option
→ load command rejected
→ no model instance created
→ no request sent
→ no semantic or truncation result
```

This result says nothing new about:

- Gemma semantic quality;
- the v1.2 category/state contract;
- the 1024-token completion budget;
- runtime throughput after a successful load.

The generated result record correctly shows `classification: not_run`, though its generic error section did not surface the load error directly. The raw evidence is authoritative for this failure.

## Evidence integrity and restoration

Verified:

```text
workflow exit: 1
load exit: 1
cleanup/unload exit: 0
post-cleanup LM Studio process list: []
product tests: 64 passed
manifest: verified=True, count=63
```

No product source, dependency, semantic contract, model selection, or external target was changed.

## Selected correction

Preserve the failed evidence unchanged. Do not overwrite or delete it.

Create a new dated correction directory and keep every diagnostic variable frozen except the load-command defect:

```text
remove unsupported --no-speculative-draft-simple
```

Keep:

```text
Gemma E4B Q4_K_XL
4096 context
--gpu max
parallelism 1
TTL 900
identifier upgradepilot-gemma-e4b-smoke
--no-speculative-draft-mtp
v1.2 prompt and exact source
flat schema and domain validators
max_tokens 1024
temperature 0
seed 0
one non-streaming response
no Instructor and no retries
GPU guard: used <= 2000 MiB and free >= 6000 MiB
```

The corrected runner must still:

1. refuse to overwrite prior evidence;
2. classify `finish_reason` before inner JSON parsing;
3. run the exact ambiguity case once;
4. stop after the one response;
5. preserve load, snapshots, request, response, reasoning/logs, validation, unload, tests, and hashes;
6. unload only the identifier it loaded;
7. stop before repetitions, contrast cases, other models, Instructor, networking, or product integration.

## Decision after the corrected run

- complete correct output → return to the v1.2 Gate A repetition plan;
- complete semantic failure → review the remaining prompt tension or compare Qwen under the same frozen contract;
- truncation at 1024 → evaluate reasoning-mode control or prompt simplification as a separate diagnostic;
- resource guard failure → identify the external GPU consumer;
- another load failure → classify the exact CLI/runtime cause before changing another variable.
