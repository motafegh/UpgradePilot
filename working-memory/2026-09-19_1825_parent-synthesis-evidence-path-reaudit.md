# Parent Synthesis — End-to-End Evidence-Path Re-Audit

**Opened:** 2026-09-19, 18:25 (session-local time)  
**Session status:** ACTIVE  
**Primary operation:** cross-responsibility analysis/review (read-only product review)  
**Method:** canonical A → B → C → D → E Learning-by-Doing, composed with repository-audit and working-memory procedures  
**Repository:** `motafegh/UpgradePilot`, `main`; initial observed source head `0201069d91f2d2bc776b84616870fe0926386f3f`; documentary updates thereafter  
**Controlling parent plan:** [`../plans/OVERALL_EVIDENCE_SUFFICIENCY_AND_MAINTAINER_ACTION_SYNTHESIS_PLAN.md`](../plans/OVERALL_EVIDENCE_SUFFICIENCY_AND_MAINTAINER_ACTION_SYNTHESIS_PLAN.md)  
**Live-state owner:** [`../MEMORY.md`](../MEMORY.md)  
**Technical closure:** [`2026-09-18_cycle3-runtime-strengthening-build.md`](2026-09-18_cycle3-runtime-strengthening-build.md)  
**Parallel learning:** [`2026-09-19_cycle3-integrated-learning-review.md`](2026-09-19_cycle3-integrated-learning-review.md)

## Formal user decision and action boundary

Ali explicitly selected and STARTED the parent evidence-sufficiency and maintainer-action synthesis re-audit as the MAIN workstream on 2026-09-19. Closed Cycle-3 implementation must not be reopened without a demonstrated regression; remaining Cycle-3 integrated learning continues in another conversation independently and is not a main-product continuation gate. Use the product-flow reconstruction itself to learn actual source/types/data/control/evidence flow and learn each relevant gap as it appears, with real tests and representative real cases where available. Preserve important evidence, assumptions, reasoning, surprises, proof limits, decisions, and A/B/C/D/E progress progressively in this record.

**Authorization:** read-only product source/test review and explanation, plus requested working-memory and live-state coordination changes. No source/test, accepted plan/spec/ADR, simulation, or external-target mutation, and no automatic Build admission merely because a review identifies a candidate gap. `MEMORY.md` alone owns the current project position. Root governance/Charter/Operating Guide and accepted synthesis/decision-model specifications remain controlling.

## Session focus (not a second plan)

1. Reconstruct the *actually reachable* PR → application producer → typed investigation → standalone synthesis evaluator → current CLI/report flow, including source contexts, CI/runtime, Target, upstream, impact, conditional investigations, missing/unsupported/failure states, and normal-path coupling. Learn one coherent seam at a time, not a ceremonial file-by-file course.
2. Compare material action-relative prerequisites with real normal producers and composition. Classify each gap as false/misattributed evidence; missing evidence; available-but-lost/miscomposed evidence; deliberate conservative coverage; unimplemented synthesis permission; or operational failure outside typed result. Distinguish source observations from tests and from prior plan hypotheses.
3. Compare only evidence-supported next responsibilities: exact consuming-job → Target integration, precise runtime version/artifact evidence, exact target-wheel compatibility producer, and any newly demonstrated higher-priority gap. Evaluate alternatives, owner, proof, complexity and stopping conditions before selecting one.
4. Use the normal Planning/Design or Build route for any later proposed mutation; do not turn the parent re-audit itself into unapproved implementation.

## Progressive engineering record

### Entry and A — learning/orientation (DONE for first bounded trace)

- Read root governance, Charter, Operating Guide, current MEMORY, parent synthesis plan, accepted synthesis/decision-model specifications, relevant closed Cycle-3 and independent learning records, and Audit/Learning-by-Doing/Working-Memory Skills. Current `main` was `0201069d91f2d2bc776b84616870fe0926386f3f` before our documentary updates.
- Recorded prior hosted technical closure: Cycle 3 focused 76/76 and full deterministic suite 604/604, run `35448172928`. This is historical executable proof, not a fresh test performed in this review and not maintainer-action or update-safety proof.
- Orientation model: source identity/acquisition → dependency/source contexts → CI and Target and upstream/impact producers → typed `PublicPullRequestInvestigation` → *separate* action evaluator → current presentation. Primary learning/ownership responsibility is to trace what is actually joined, what is merely adjacent, and which proposition fails at which owner boundary. Detailed parser internals remain deferred absent contrary evidence.

### B — actual source/test trace (IN PROGRESS; first integration seam characterized)

**1. PR and dependency entry (`src/upgradepilot/investigation.py`, `PublicPullRequestInvestigation` and `investigate_public_pull_request`):** Application calls `get_pull_request` then `get_changed_files` then `analyze_dependency_change`. On an accepted `DependencyChangeAnalysis`, carries `DependencyVersionChange` and exact `source_contexts`; otherwise carries `DependencyChangeProblem` and empty contexts. Only the supported change enters the CI/PyPI/upstream/impact acquisition branches. The result is a typed `PublicPullRequestInvestigation` containing the fields collected, including possible `None`/empty states. Reading the source establishes the control path and typed return; it does not prove all exceptions are converted into that typed result.

**2. CI and Target composition — confirmed contract and transfer (`src/upgradepilot/ci/consumption.py`, `ci/dependency_exercise.py`, `investigation.py`, `target/artifact_environment.py`):** `StaticDependencyConsumptionEvidence` already records `workflow_path`, `workflow_revision`, `job_key`, `step_source_index`, `source_path`, package identity, state and mechanism; provider/static observation is separate from runtime strength. For a supported dependency change, application acquires exact-head workflow runs/jobs and workflow definition, forms `WorkflowDependencyCoverageInput`, and evaluates CI. **Only after an `ArtifactServiceabilityImpactCandidate` exists** does it call `_compose_target_artifact_environments` (and only supported `direct_requirements` consumptions enter this Target path). Composition checks one-to-one workflow order/path, exact workflow path/revision, and unique matching `RequirementsFileDependencyContext` by source path/revision/package. It then calls `interpret_target_artifact_environment(definition, dependency_source_file=source_context.source_path)` without `consumption.job_key`. Target reparses the workflow and `_select_target_job` accepts only exactly one job, returning `ambiguous_target_job_selection` for multiple jobs. The composition deduplication key `(workflow_revision, workflow_path, source_context.source_path)` also omits `job_key`: distinct supported consumptions of the same source in different jobs do not create distinct Target relationships. No currently observed wrong positive claim is asserted; this is an available-but-not-transferred/recomposed relationship plus a deliberate one-job Target contract.

**3. Nearest focused and application test evidence (`tests/test_target_artifact_environment.py`, `tests/test_investigation.py`):** Focused Target test `test_multiple_jobs_are_target_selection_ambiguity_not_parser_failure` protects the existing conservative two-job result; single-job test preserves runner/Python/install-declaration facts and explicit `exact_wheel_compatibility_state='unresolved'`. Application test `test_target_artifact_environment_uses_supported_direct_requirements_relationship` establishes supported CI consumption → one-job Target evidence in a purpose-built fixture. Application test `test_multi_job_target_ambiguity_is_preserved_despite_ci_job_relevance` supplies a purpose-built `test` job installing requirements and unrelated `lint` job, confirms CI static consumption is supported, Target returns `ambiguous_target_job_selection`, artifact applicability remains unresolved. Application test `test_target_artifact_environment_stays_inactive_without_real_candidate` confirms no Target assessment when artifact candidate is absent. These are current source and test assertions, NOT a newly executed test run, a representative public-PR proof, or an accepted future API design. Distinguish the illustrative teaching YAML from these actual repository fixtures.

**4. Investigation → synthesis evaluator (`src/upgradepilot/maintainer_action.py`; `tests/test_maintainer_action.py`):** `MaintainerAction = Literal['abstain']`; standalone `synthesize_maintainer_action(investigation)` preserves the same `PublicPullRequestInvestigation` and returns reasons, residual uncertainty, limitations and claim limits. Focused tests build fixture investigations (including synthetic/example repository and runtime-correlated CI state) and assert abstention; these are contract tests, not normal public-PR non-abstention integration proof.

**5. Current human CLI/presentation (`src/upgradepilot/cli.py`; `tests/test_cli.py`):** `main` calls `investigate_public_pull_request` then `_print_investigation` and returns exit code 0 for a returned investigation. It does not invoke `synthesize_maintainer_action`. Current CLI renders an evidence report, not a maintainer-action report; CLI tests explicitly assert the absence of `Maintainer recommendation` for an artifact case. The typed evaluator exists separately. Classify as not-yet-integrated synthesis presentation / incomplete product path under the parent plan, not proof that action permission should be loosened or that CLI feature work must precede the action premise review. GitHub acquisition/response exceptions can be operational error exits, not automatically typed semantic abstentions.

**6. Current flow correction:** Intended product horizon `investigation → synthesis → maintainer-action output` is NOT today's normal CLI flow. Observed current CLI path is `PR → investigation → evidence report`; separately callable `investigation → abstention-only evaluator`. No currently inspected path proves substantive non-abstention action reachability.

**Still unverified:** public-case evidence for this exact multi-job Target seam; whether another interface invokes the evaluator; remaining producer branches; action-relative impact of obtaining the exact consuming-job Target facts compared to genuinely missing runtime installed-version/wheel/compatibility evidence. Do not rank a next implementation from one seam alone.

### C — progressive preservation (CURRENT CHECKPOINT UPDATED)

- Created a distinct main workstream record and reconciled `MEMORY.md` to the selected re-audit while retaining Cycle-3 integrated learning as parallel; canonical live position remains `MEMORY.md`.
- Captured material distinction of producer-held `job_key`, application handoff dropping job specificity, Target's conservative whole-workflow selection, deduplication across distinct jobs, and artifact-candidate gating. Marked the positive correctness boundary and limits of source/test evidence; none is an authorized repair.

### D — post-action learning / user-owned check (FIRST CHECK COMPLETED; SOURCE-TRACE CHECK NEXT)

- Ali originally lacked the CI/Target prerequisites; taught distinction between CI consuming-job evidence, Target static runner/Python job facts and independent one-job selection rule. Illustrative two-job YAML was synthetic. Ali then correctly predicted `ambiguous_target_job_selection` and rejected exact-installed-version inference; explanation of why was initially uncertain and clarified: Target sees both jobs but is not passed the already-supported exact consuming-job identity. Understanding of expected outcome/non-proof shown; source-level trace and changed-case independence not yet demonstrated.
- Next meaningful check, after reviewing the actual application test/source, can ask what would happen if *both* jobs independently and validly consumed the same dependency source but used different runner/Python environments; reason about why deduplication by only workflow/source would lose a distinct environment relationship. Explain any unfamiliar premise before evaluating.

### E — next slice orientation (ACTIVE)

- The CI→Target contract and test contrast is now source-backed. Before choosing a Build direction, inspect a representative public case where relevant and trace next evidence-critical seam: how published-wheel artifact candidates plus partial static Target results affect applicability, whether any normal producer creates exact target wheel-tag witness, and which specific maintainer action would materially benefit. Keep separate learning and stronger-evidence needs in the same progression record. Do not resume Cycle-3 internals or implement a job-selector repair automatically.

## Current session handoff

Main re-audit ACTIVE; documented exact CI consuming-job-to-Target handoff and existing focused/application tests. CI `job_key` is present in typed evidence but omitted by the application-to-Target API; deduplication ignores job; Target rejects multi-job workflows conservatively; Target assessment requires an artifact candidate and supported direct requirements relationship. Ali's first conceptual check passed after prerequisite teaching; user-owned source/changed-case reasoning remains open. Next compare the actual artifact-applicability consumer and candidate/Target proof needs before selecting any product responsibility. No product tests were newly run and no source/tests/specifications/plans were changed.

**Activated procedures:** `UP-SKILL:upgradepilot-repository-audit`; `UP-SKILL:upgradepilot-learning-by-doing`; `UP-SKILL:upgradepilot-working-memory`.
