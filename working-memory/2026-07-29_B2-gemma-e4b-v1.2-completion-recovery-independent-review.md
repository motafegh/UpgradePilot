# B2 Gemma E4B v1.2 Completion-Recovery Independent Review

**Date:** 2026-07-29  
**Record type:** Dated independent review; non-controlling  
**Reviewed commit:** `154d83a3ad0741dc60262f0deaafed07d0536669`  
**Reviewed result:** [`2026-07-29_B2-gemma-e4b-v1.2-completion-recovery-load-flag-correction-result.md`](2026-07-29_B2-gemma-e4b-v1.2-completion-recovery-load-flag-correction-result.md)  
**Reviewed raw evidence:** [`evidence/2026-07-29-gemma-e4b-v1.2-completion-recovery-load-flag-correction/`](evidence/2026-07-29-gemma-e4b-v1.2-completion-recovery-load-flag-correction/)

## Verdict

The corrected completion-recovery run is valid and passed its complete bounded responsibility.

```text
clean pre-load control: passed
model load: passed
complete response: passed
finish reason: stop
structured shape: passed
state/claims/reasons invariant: passed
category/change-state matrix: passed
exact source grounding: passed
frozen semantic oracle: passed
model unload/restoration: passed
product tests: 64 passed
evidence manifest: 106/106 verified
```

This result counts as **state-contract v1.2 Gate A repetition 1 of 3** for the exact ambiguity source. It does not establish broader semantic reliability, model adoption, evidence sufficiency, safety, or a maintainer action.

## Runtime and resource review

The required pre-load control band was met:

```text
GPU used: 1431 MiB
GPU free: 6588 MiB
GPU utilization: 9%
GPU temperature: 49 C
loaded LM Studio models: none
```

Gemma E4B loaded successfully in 4.22 seconds. After load, the observed GPU state remained comparable to the earlier accepted control:

```text
GPU used: approximately 4811 MiB
GPU free: approximately 3208 MiB
```

The response completed normally:

```text
finish reason: stop
prompt tokens: 543
completion tokens: 458
reasoning tokens: 360
total latency: 7.261621 seconds
LM Studio throughput: 66.9463 tokens/second
LM Studio time to first token: 0.392744 seconds
```

This removes the resource contamination and completion truncation that invalidated the earlier v1.2 attempt.

## Semantic result

Source:

```text
Compatibility behavior was adjusted for older environments.
```

Observed structured result:

```json
{
  "state": "resolved",
  "claims": [
    {
      "category": "interface_or_behavior_change",
      "subject": "compatibility behavior",
      "change_state": "changed_unspecified",
      "source_quote": "Compatibility behavior was adjusted for older environments."
    }
  ],
  "unresolved_reasons": []
}
```

The result matches the frozen v1.2 oracle exactly at every material field:

```text
state: resolved
category: interface_or_behavior_change
change state: changed_unspecified
source quote: exact contiguous source span
unresolved reasons: zero
```

The preserved reasoning also followed the intended boundary:

- rejected `compatibility_assurance` because the source gives no assurance;
- rejected support added/dropped because no support direction is stated;
- selected `changed_unspecified` because a behavior change is explicit and its direction/effect is absent;
- selected `resolved` because the minimum explicit meaning can be represented without inventing the missing direction.

This is stronger evidence than a merely schema-valid output because the deterministic validator confirmed structure, state consistency, category/change-state compatibility, grounding, and the frozen semantic oracle.

## Restoration and evidence quality

After inference:

```text
unload exit: 0
LM Studio loaded models after cleanup: []
LM Studio status: No Models Loaded
product tests: 64 passed
manifest verification: 106/106 passed
```

One documentation defect remains in the generated result record: the visible raw-evidence label names the correction directory, but the Markdown link target still points to the first completion-recovery directory. This review records the correct evidence path above. The evidence itself is intact and its hashes passed.

The bundle also contains a generated `__pycache__` bytecode file. It is harmless to this result, but future evidence runners should suppress or exclude Python bytecode to reduce non-essential evidence noise.

## Decision

The completion-recovery blocker is closed.

Gemma E4B has now demonstrated one complete and correct v1.2 ambiguity classification under the accepted runtime baseline. The 1024-token completion budget becomes the frozen baseline for the remainder of this v1.2 diagnostic because the 512-token budget previously ended before structured output.

No model/provider/dependency adoption follows from one correct case.

## Selected continuation

Count this result as Gate A repetition 1. Under the same frozen configuration and clean GPU preflight:

1. run the exact ambiguity source two additional times;
2. require both to return `resolved + interface_or_behavior_change/changed_unspecified`, exact grounding, and zero unresolved reasons;
3. if either repetition fails or truncates, stop and preserve evidence;
4. if Gate A reaches 3/3, run the frozen Gate B contrast suite once per case;
5. only if Gate B passes, run the frozen Gate C conflicting-support case;
6. stop and push before the broader corpus, pytest release input, Instructor, Qwen, Gemma 12B, networking changes, or product integration.

Keep frozen:

```text
Gemma E4B Q4_K_XL
4096 context
--gpu max
parallelism 1
TTL 900
identifier upgradepilot-gemma-e4b-smoke
--no-speculative-draft-mtp
state-contract v1.2 prompt
flat JSON Schema
category/change-state matrix
frozen cases and oracles
max_tokens 1024
temperature 0
seed 0
non-streaming endpoint
no Instructor
no retries
pre-load GPU used <= 2000 MiB
pre-load GPU free >= 6000 MiB
no model loaded before the run
```

The semantic interpretation method remains experimental and unadopted until the selected corpus and decision-effect gates are completed and reviewed.
