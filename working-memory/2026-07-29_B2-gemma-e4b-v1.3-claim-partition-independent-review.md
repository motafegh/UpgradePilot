# B2 Gemma E4B v1.3 Claim-Partition Independent Review

**Date:** 2026-07-29  
**Reviewed evidence commit:** `151f015edb698b95d9da69a7a463c7326818cb83`  
**Responsibility:** Verify whether the single v1.3 claim-partition clarification corrected the exact compatibility-assurance over-extraction failure and determine the justified product consequence.

## Verdict

The v1.3 diagnostic passed 3/3 for the frozen sentence:

```text
This release remains backward compatible with the previous patch release and requires no migration.
```

Every repetition returned:

```text
state: resolved
exactly one claim
category: compatibility_assurance
change_state: compatibility_assured
exact full-sentence source quote
zero unresolved reasons
```

All three runs completed with `finish_reason: stop`, passed structural validation, and had semantic-oracle evaluation explicitly performed and passed.

## Independent raw-evidence checks

The generated summary was not accepted alone. The preserved raw evidence was checked for:

- clean preflight: server reachable, no loaded model, GPU used `1404 MiB`, GPU free `6615 MiB`;
- frozen runtime controls: temperature `0`, seed `0`, `max_tokens=1024`, non-streaming;
- empty runtime, structure, and semantic error lists;
- exact grounded single-claim output;
- `semantic_evaluated=true` and `classification=complete_correct`;
- post-unload LM Studio process list `[]`;
- product tests: `64 passed`;
- manifest verification: `134/134 passed`.

The three repetitions used the same prompt, schema, and request identities. Their token counts differed, but their structured semantic result was identical.

## What this establishes

The added rule successfully prevents this exact sentence from being split into an invalid second `support_boundary_change / compatibility_assured` claim.

It supports retaining this claim-partition rule in the experimental extraction contract:

> Emit one claim per distinct supported category/change-state proposition, not one claim per clause or phrase. Clauses that jointly support the same proposition are combined into one claim.

## Detected gaps

This result does not establish:

1. broad release-note reliability;
2. reliable extraction of Python-version support drops;
3. a normalized Python `X.Y` field usable by deterministic comparison;
4. target-repository relevance;
5. compatibility or safety for a Dependabot update;
6. evidence sufficiency or stopping;
7. a maintainer action;
8. model or product adoption.

The remaining frozen Gate B cases and Gate C were not completed under v1.3. That is accepted for now because they do not resolve the more immediate product gap: no target-side evidence exists against which an upstream claim can be used.

## Why prompt tuning is paused

Further sentence-by-sentence prompt changes would optimize the extractor in isolation before UpgradePilot has shown that the extracted claim changes any target-specific evidence result.

The project charter requires experiments to answer an observed product limitation and to justify complexity against a simpler baseline. The next observed limitation is not another wording error; it is the absent connection between an attributed upstream claim and exact-revision target-repository evidence.

Therefore:

```text
local LLM prompt tuning: paused
v1.3 artifacts: retained as experimental evidence
model adoption: not approved
product integration: not started
```

Prompt or model work may resume only when a concrete target-relevance case exposes an extraction blocker that deterministic validation cannot solve.

## Product-direction consequence

The next bounded responsibility is target Python declaration relevance, beginning with an explicit upstream `support_dropped` claim and exact-head `[project].requires-python` evidence.

This direction is defined by [`../plans/B2_TARGET_PYTHON_SUPPORT_RELEVANCE_PLAN.md`](../plans/B2_TARGET_PYTHON_SUPPORT_RELEVANCE_PLAN.md).