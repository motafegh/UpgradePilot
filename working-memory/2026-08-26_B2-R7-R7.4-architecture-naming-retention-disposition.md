# Working Memory — B2 R7.4 Architecture, Naming, and Retention Disposition

**Date:** 2026-08-26  
**Status:** R7.4 COMPLETE TO REMOTE OWNERSHIP/RETENTION-REVIEW DEPTH; R7.5 NEXT  
**Execution branch:** `main`  
**Parent R7 record:** `2026-08-26_B2-R7-acceptance-cleanup-and-baseline-closure.md`  
**Findings register:** `2026-08-26_B2-R7-findings-register.md`

## 1. Purpose

R7.4 decides what current responsibility actually justifies before R7.5 edits source. It does not treat current references, old tests, comments, or historical design as retention authority.

Decision rule:

```text
current use / old test / historical API
!= retention justification

trace current producer → orchestration → consumer
+ identify exact present responsibility
+ identify concrete current failure if removed
→ KEEP / MOVE / NARROW / REMOVE
```

No runtime validation is claimed here. R7.4 is a remote source/caller/ownership decision audit.

## 2. Normal current CI owner established

The ordinary product route is coverage-oriented:

```text
investigation.py
→ WorkflowDependencyExerciseInput (current stale name)
→ evaluate_dependency_ci_coverage(...)
→ WorkflowDependencyCoverageResult / DependencyCICoverageResult
→ PublicPullRequestInvestigation.ci_coverage_result
→ CLI/application
```

`evaluate_dependency_ci_exercise(...)` and `inspect_workflow_commands(...)` are not on that route. Current source itself labels both as temporary legacy surfaces retained for migration, and R6 migration is complete to remote source/orchestration depth.

## 3. R7.4 disposition table

### D-001 — `evaluate_dependency_ci_exercise(...)` and legacy exercise result types

**Disposition:** REMOVE IN R7.5.

Includes the legacy-only result surface:

```text
WorkflowDependencyExerciseResult
DependencyCIExerciseResult
evaluate_dependency_ci_exercise(...)
legacy evaluator-only helper path
```

Reason:

- no ordinary production caller remains;
- current application uses `evaluate_dependency_ci_coverage(...)`;
- source comments explicitly say the legacy path was temporary until migration;
- remaining references are legacy tests, source-topology protection, plans/working memory/learning history;
- keeping it creates two apparent CI authorities for one migrated responsibility.

Historical documents remain historical and are not rewritten merely because production removes the compatibility surface.

### D-002 — `inspect_workflow_commands(...)` and `WorkflowCommandEvidence`

**Disposition:** REMOVE IN R7.5.

Reason:

- the helper exists for the pre-coverage combined install→invocation rule;
- normal current static CI evidence uses `inspect_workflow_dependency_evidence(...)` with consumption and invocation separated;
- repository caller pressure outside the legacy evaluator is tests/history, not a present product boundary;
- preserving the helper would preserve obsolete combined semantics after the owning evaluator is removed.

Current `inspect_workflow_dependency_evidence(...)`, `WorkflowStaticDependencyEvidence`, `StaticDependencyConsumptionEvidence`, and `DirectPackageInvocationEvidence` remain current owners and are KEEP.

### D-003 — `PublicPullRequestInvestigation.ci_exercise_result` read alias

**Disposition:** REMOVE IN R7.5.

Reason:

- the authoritative field is `ci_coverage_result`;
- current source labels the alias transitional and explicitly names R7 cleanup;
- current CLI/application uses `ci_coverage_result`;
- remaining live references found are old tests/history rather than a declared compatibility contract.

Tests that still read the alias should migrate to `ci_coverage_result`; they must not become retention authority for the alias.

### D-004 — `DependencyChangeAnalysis.direct_requirements_install_path` compatibility projection and investigation result field

**Disposition:** REMOVE IN R7.5.

Reason:

- `source_contexts` is the current downstream source of truth;
- direct requirements evidence is now interpreted from `RequirementsFileDependencyContext` inside the coverage/static-evidence path;
- current source explicitly describes the property as an old-style compatibility projection retained until CI migration;
- `investigation.py` still copies it into `PublicPullRequestInvestigation`, but the normal current coverage route does not consume it;
- repository caller pressure is legacy evaluator/tests/history, not a separate supported product responsibility.

Removing this projection must not remove `RequirementsFileDependencyContext` or direct-requirements consumption semantics; those are current and remain KEEP.

### D-005 — `WorkflowDependencyExerciseInput`

**Disposition:** NARROW/RENAME IN R7.5; responsibility remains KEEP.

Target direction:

```text
WorkflowDependencyExerciseInput
→ WorkflowDependencyCoverageInput
```

Reason:

- this object is still required by the normal production route;
- it carries exact runtime run/jobs, exact workflow definition, and already-composed project-environment consumption into the coverage aggregator;
- after legacy evaluator removal, “ExerciseInput” falsely suggests the obsolete combined exercise API is still the owning proposition.

Do not remove the input object merely because its name is stale.

### D-006 — `external_consumptions` field/parameter naming

**Disposition:** NARROW/RENAME IN R7.5; semantic collection remains KEEP.

Preferred current name:

```text
project_environment_consumptions
```

Reason:

- normal investigation derives these specifically through R6 project-environment composition;
- direct requirements consumptions are derived inside the workflow static-evidence owner from `source_contexts`;
- “external” is implementation-history language and is too broad: it makes arbitrary caller-supplied consumption injection look like a product contract;
- the collection itself is needed so R3→R4/project-source→R5 evidence reaches coverage without being recomputed.

The rename must remain narrow; do not introduce a generic evidence-injection abstraction.

### D-007 — module `ci/dependency_exercise.py`

**Disposition:** KEEP.

Reason:

- even after the legacy evaluator is removed, the module still owns coverage classification plus the stronger direct-exercise axis and their separation;
- renaming the whole module would create import/test churn without changing responsibility;
- the stale *types/functions* can be removed/narrowed while the module remains a coherent owner.

### D-008 — public `evaluate_uv_selected_environment_membership(...)` contract and legacy membership result types

**Disposition:** REMOVE IN R7.5 after unique current-proof regressions are retained/migrated.

Includes:

```text
UvSelectedEnvironmentMembership
UvSelectedEnvironmentMembershipState
UvMembershipKind
evaluate_uv_selected_environment_membership(...)
project-metadata membership-only helpers that have no current R4 responsibility
```

Reason:

- repository-wide caller tracing found no production caller of the public evaluator;
- current R4 public contract is `evaluate_uv_selected_root_reachability(...)`;
- R4 deliberately narrowed the proposition from broad selected-environment membership to explicit selected-root reachability;
- old membership tests/history are evidence to inspect, not authority to retain the obsolete public proposition.

Before deleting legacy tests, R7.5 must inspect them for any unique case that still protects the current R4 reachability contract. Such a case should migrate to `test_uv_selected_root_reachability.py` or the appropriate current owner rather than preserving the old API.

### D-009 — reachability projection/resolution mechanics currently private in `uv_membership.py`

**Disposition:** MOVE IN R7.5 to the current R4 owner; do not delete.

Current `uv_reachability.py` imports private mechanics including:

```text
_MAX_PATH_DEPTH
_MAX_VISITED_STATES
_ReachabilityEdge
_ReachabilityLock
_ReachabilityPackage
_build_reachability_lock
_normalize_source_path
_resolve_edge
_workspace_source_path
```

These mechanisms implement current selected-root reachability: projection of admitted lock records into graph/root fields, deterministic edge resolution, workspace-source binding, and traversal safety bounds.

The simplest current ownership is to move the needed mechanics into `uv_reachability.py` rather than create a new generic package-manager/graph abstraction. `uv_lock_structure.py` remains the shared external lock-structure admission owner; `uv_reachability.py` owns reachability-specific projection and traversal semantics.

This is recorded as F-005 because the legacy module currently acts as an accidental implementation host for the preferred R4 contract.

### D-010 — source-topology and legacy tests

**Disposition:** NARROW/MIGRATE IN R7.5.

- `tests/test_source_topology.py` should protect the current coverage/reachability owners, not import a removed legacy evaluator merely to keep it alive.
- `tests/test_ci_dependency_exercise.py` should be removed with the legacy evaluator after any still-current proof pressure is confirmed covered by coverage tests.
- legacy uv membership tests should be removed only after unique current R4 proof cases are migrated.
- normal investigation, CLI, R6, coverage, S001/S011/S005, F-004 provenance, and current R4 tests remain the acceptance spine.

## 4. F-005 — Legacy uv membership API is an accidental host for current R4 mechanics

**Discovered:** R7.4 architecture/retention review  
**Disposition:** FIX IN R7.5 BY OWNER MOVE + LEGACY PUBLIC-SURFACE REMOVAL  
**Blocker before R7.8:** YES FOR CLEAN OWNERSHIP/CANDIDATE FREEZE; not a currently demonstrated false-support bug  
**Risk class:** responsibility split / transitional implementation host  
**Owning area:** `dependency/uv_reachability.py`

Exact current shape:

```text
preferred public R4:
uv_reachability.py

but

uv_reachability.py
→ imports private reachability graph/projection helpers
→ from legacy uv_membership.py

legacy public uv membership evaluator
→ no production caller found
```

This means deleting `uv_membership.py` blindly would break current R4, while retaining the whole module would preserve an obsolete public proposition and duplicate ownership language.

Selected repair direction:

```text
move current reachability-specific helpers
→ uv_reachability.py

retain shared external lock admission
→ uv_lock_structure.py

remove obsolete public membership evaluator/result API
→ remove/migrate legacy tests after unique proof-case review
```

Do not create a new generic graph abstraction unless R7.5 uncovers a second present responsibility that genuinely needs one.

## 5. Explicit R7.4 KEEP set

R7.4 found no retention reason to disturb these current owners:

```text
evaluate_dependency_ci_coverage(...)
WorkflowDependencyCoverageResult / DependencyCICoverageResult
inspect_workflow_dependency_evidence(...)
WorkflowStaticDependencyEvidence
StaticDependencyConsumptionEvidence
DirectPackageInvocationEvidence
RequirementsFileDependencyContext direct-install semantics
derive_project_environment_consumptions(...)
WorkflowProjectEnvironmentSource
observe_project_environment_selection(...)
evaluate_project_source_environment_membership(...)
evaluate_uv_selected_root_reachability(...)
uv_lock_structure.py shared structural admission
ci/dependency_exercise.py module owner
PublicPullRequestInvestigation.ci_coverage_result
```

F-004 checkout-provenance guard remains current R6 responsibility and is KEEP; runtime proof remains pending R7.9.

## 6. R7.5 entry decisions

R7.5 is now authorized by the R7 plan to implement only the bounded cleanup/corrections justified by recorded evidence. Its initial order should be:

```text
1. F-002 proof-calibration fix
   preserve unavailable required project-root evidence as unresolved

2. F-003 cleanup according to D-001..D-007/D-010
   remove legacy CI API residue
   narrow current coverage names

3. F-005 owner move according to D-008..D-010
   move current R4 mechanics
   retire obsolete uv membership API after unique-test migration

4. F-001 explicit disposition
   currently lean toward ACCEPT AS KNOWN BOUNDED LIMITATION unless R7.5 interaction/evidence strengthens it

5. Re-audit F-004 while touching workflow/coverage code
   do not weaken checkout provenance
```

R7.5 must inspect actual diffs after each bounded cleanup cluster and stop if a cleanup creates new proof ambiguity or broadens product responsibility.

## 7. R7.4 closure

```text
R7.4 ARCHITECTURE/NAMING/RETENTION REVIEW: COMPLETE TO REMOTE DEPTH
RUNTIME VALIDATION: NOT PERFORMED / PENDING R7.9
PRODUCT SOURCE CHANGED BY R7.4: NO
RETENTION DECISIONS RECORDED: YES
NEW FINDING: F-005
NEXT: R7.5 bounded remote cleanup/finding dispositions
```
