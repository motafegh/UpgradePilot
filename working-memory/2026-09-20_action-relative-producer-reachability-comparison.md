# Action-relative producer/reachability comparison — 2026-09-20

**Role:** Dated analysis/design and Learning-by-Doing record for the selected responsibility after closed F3/F9/F11. `MEMORY.md` alone owns live selection. Comparison does not authorize source/test Build, alter accepted action semantics, or make a non-abstention action currently reachable.

## A — orientation: DONE

Ali requested immediate continuation with canonical A → B → C → D → E and progressive preservation. Start with a concrete maintainer decision, separate **positive action permission** (what must be established) from **normal producer reachability** (what the application really establishes), and trace earliest evidence owner → application composition → action-consumer requirement → missing premise and close defeater. Learn with concrete changed cases, not a large technical lecture.

Current baseline: `MEMORY.md` selected the action-relative comparison after closed F11. `plans/END_TO_END_PRODUCT_FLOW_LEARNING_AND_EVIDENCE_TO_ACTION_EXECUTION_PLAN.md` requires comparing action-specific premises and selecting only one material next responsibility. The accepted `docs/specifications/UPGRADEPILOT_MAINTAINER_ACTION_SYNTHESIS_SPECIFICATION.md` defines independent positive conditions for targeted checks, investigate, block, defer, merge and honest abstention; actions are not severity levels. `src/upgradepilot/maintainer_action.py` currently implements `Literal["abstain"]`; `src/upgradepilot/investigation.py` constructs a typed investigation but does not normally pass it through a non-abstention action evaluator. Historical September 11 reachability notes describe an older code baseline and must not be used as current implementation proof after later correctness/Cycle changes.

**Initial main revision before this record:** `e2cf430b54be64e077f0ff2e8faa2409fce22398`. No product source, tests, stable specifications or execution plans edited in this comparison.

## B — action-relative comparison: DONE (source/accepted-semantics/design evidence, not new runtime proof)

### Common evidence spine and permission distinction

Normal `PublicPullRequestInvestigation` carries exact PR/dependency identity, CI consumption/coverage, PyPI old/proposed release facts, upstream/Python support when sourced, mechanism-specific applicability, and partial artifact Target results. These facts do not automatically authorize a maintainer action. Source: `src/upgradepilot/investigation.py`; `src/upgradepilot/maintainer_action.py`. Prior F3/F9 hosted verification is for implementation at its exact revision, not a proof of a new action.

| Action and useful case pressure | Positive premises vs current normal production | Defeater / selection implication |
| --- | --- | --- |
| `merge after normal review` (S004/S005; S009/S010 context pressure) | Needs positive, explicitly bounded candidate and repository-context coverage in addition to exact identity and resolved material concerns. Current mechanisms only inspect selected Python-support and artifact candidates and do not establish broad or explicit favorable coverage. | F7 is not merely a small extra test; absence of an observed concern or green CI is not positive closure. Do not select broad favorable discovery first. |
| `run targeted checks` (S006 one exact exception-behavior discriminator; artifact fallback S008 as a separate possible question) | Needs a specific unresolved target-relevant question, a concrete maintainer-performable check with meaningful possible outcomes, and no better already justified UpgradePilot-owned investigation. Current Python selector selects a *product-owned read-only target declaration acquisition*; it is not a maintainer check. Current artifact candidate is target-agnostic until exact environment applicability is established. | Do not emit "test more" merely from unresolved applicability. A correct target-environment relationship is a useful prerequisite to a future artifact-oriented check, but F4 alone does not produce or authorize the check. |
| `investigate` (Cactus #198 screening, multi-branch artifact/X.509 pressure) | Requires target-relevant concern and grounded broader/adaptive inquiry with scope, stopping/pruning logic and no independent block. Normal current investigation does not derive that broad maintainer inquiry across those mechanisms. | Multiple candidate states or generic uncertainty are insufficient. Selecting a broad new planner here would bypass missing basic producers. |
| `block` (S003 demonstrated proposal-level failure/conflict) | Requires a proven exact-proposal hold. For Python support, accepted §7.4.1 additionally requires authoritative support exclusion, exact affected supported-environment obligation, changed dependency actually required there, and no relevant alternative/proposal correction. Current Python `requires-python` overlap alone is insufficient. For wheel loss, source fallback and missing exact target tags prevent a block claim. | S003 is TypeScript/npm evidence from a different mechanism and is not current Python normal-path proof. No block permission follows just from `established_applicable`, red/green CI, support-drop text, or disappeared wheel tag. |
| `defer` (S012 outside history pressure) | Needs a **specific** useful outside/future responsibility and reassessment trigger, not simply an unsupported local capability. No generic future-responsibility producer is established in the normal Python/artifact path. | Do not rename missing capability as defer; preserve abstention absent independent permission. |

### F4/F5/F6/F7 comparison within a concrete artifact-oriented path

**F4: precise currently lost relation.** `src/upgradepilot/ci/consumption.py::StaticDependencyConsumptionEvidence` preserves `workflow_path`, `workflow_revision`, `job_key`, `source_path` and `state`. `src/upgradepilot/investigation.py::_compose_target_artifact_environments` accepts only `supported` `direct_requirements` consumption and exact workflow/source correspondence, but drops `consumption.job_key` when constructing the deduplication relation and when calling `interpret_target_artifact_environment(definition, dependency_source_file=...)`. `src/upgradepilot/target/artifact_environment.py::_select_target_job` independently demands exactly one workflow job, otherwise returns `ambiguous_target_job_selection`. `tests/test_investigation.py::test_multi_job_target_ambiguity_is_preserved_despite_ci_job_relevance` demonstrates a two-job workflow (`test` installs the changed requirements, `lint` does not): CI supports consumption yet Target remains ambiguous. This is safe conservatism, not false-positive evidence.

**F5: required but not cheaply inferred target compatibility.** `impact/artifact_serviceability.py` provides the exact target-owned `TargetWheelCompatibilityEvidence` contract and focused tests prove that synthetic target tags can resolve or refute an artifact candidate. Normal application calls `evaluate_artifact_serviceability_impact(candidate)` *without* target evidence; Target's current runner/Python version/install declaration has `exact_wheel_compatibility_state=unresolved`. Obtaining exact target tags requires a genuine environment-owned observation; inferring them from runner/Python labels or UpgradePilot's own runtime would invalidate the result. With source fallback, even established wheel-path loss does not establish install failure or an automatic block. F5 is a material downstream producer, but a credible exact target observation/source and its identity binding must be designed before code.

**F6:** current exact CI static↔runtime result establishes at most a bounded successfully completed correlated step, not which version or wheel was actually installed. Exact runtime artifact identity is necessary only for a selected action/proposition that specifically relies on it; generic log ingestion is not selected.

**F7:** bounded candidate/context discovery is a genuine positive-favorable-permission dependency but involves a broader product horizon; it is not established by simply iterating two current mechanism candidates. Not selected as the first one-responsibility slice.

### One selected next responsibility for a separate cycle

**Select F4 — preserve the independently established exact CI *consuming job* into the Target artifact-environment interpretation and normal investigation result.** This is not chosen simply because it is easy: the current application provably loses an existing source/job relationship in a concrete two-job test, preventing the product from identifying which declared Target environment consumes the changed dependency. A target-bound relationship is one necessary input for a future artifact-serviceability applicability/check path. Restoring it is the smallest useful bounded *composition* repair before sourcing deeper target-owned observations. It is deliberately a **precursor, not an action-unlocking implementation**; no targeted check, wheel applicability, runtime install version, block or favorable recommendation becomes justified by F4 alone.

**Build/Design entry contract:** preserve `workflow_path + exact revision + source identity + supported consumption.job_key`; safely interpret the *selected* job, reject missing/mismatching/unsupported/ambiguous keys, keep deduplication job-specific, preserve the one-job and unrelated workflow regression cases, and prove two-job positive + wrong-job/identity/unsupported negatives through normal application tests. Do not let CI's fact of *static consumption* become Target runtime proof, do not infer exact wheel tags, and do not expand matrix/reusable-workflow support. Before product implementation, inspect the exact Target job-selection API and CI source/revision trust boundary; if the selected job is not independently or compositionally safe to bind, return to bounded design rather than guessing. This selection does not grant independent Build authorization or promise that the entire maintainer-action path will be finished by F4.

**Why next:** F4 is currently demonstrable with existing exact in-repository evidence and directly restores missing target-context identity. F5 requires a new actual observation, F6 depends on a chosen runtime proposition, and F7 requires a deliberately bounded favorable coverage horizon. Reassess those **after** the selected F4 result is proven, against an exact action permission; do not turn this comparison into a four-item implementation queue.

**Source/pressure anchors:** accepted synthesis specification §§5–11; selected end-to-end plan's action-relative selection/F4 stop lines; `src/upgradepilot/investigation.py`, `ci/consumption.py`, `target/artifact_environment.py`, `impact/artifact_serviceability.py`, `impact/python_support.py`, `src/upgradepilot/maintainer_action.py`; `tests/test_investigation.py`, `tests/test_artifact_serviceability.py`; AUDIT-008 F4–F7; product-simulation S006, S008 and `INVESTIGATE_VS_BLOCK_EXISTING_EVIDENCE_REPORT_2026-09-11.md`. Simulation results are design pressure, not normal runtime action proof.

## C — preservation: DONE for analysis

Created this record at A→B, then incorporated accepted permission/current source/real-case distinctions, exact proposed one-slice scope, alternatives/defeaters and non-proof. Current code/CI was inspected through the connected GitHub repository; no new local or hosted test execution and no product mutation occurred. Update `MEMORY.md` with the meaningful comparison milestone while keeping D/E for this cycle pending before activating the separate F4 cycle.

## D — source-backed explanation and ownership check: NEXT

Teach Ali with `test` versus `lint` in a two-job workflow: CI already knows `test` installs the changed package, but Target forgets the job and sees two jobs, so cannot identify the relevant Python/runner declaration. Explain that passing the trusted job association improves *which environment* we examine, not *which wheel was actually installed* or whether to block the PR. Ask one changed-case reasoning question; no jargon recall.

## E — learner gap repair and next-slice orientation: PENDING

After the answer, repair any material confusion and record the comparison closure. Orient the **separate** F4 bounded design/build cycle starting from concrete source association; do not silently implement or admit an action in this comparison.
