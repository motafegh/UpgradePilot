# UpgradePilot Current Memory

**Last updated:** 2026-09-26  
**Authority:** sole owner of the live project position, current blockers, selected continuation, and current learning depth.

## Live position — main product workstream STARTED

- **Current responsibility:** **Effective Package-Manager Semantics — Supported Subsystem Boundary Design** (historical plan marker R2), under the wider **Runtime Dependency-State Proof** responsibility. Product goal: build on the **existing Tree-sitter-backed static command/source analysis and exact CI runtime-success correlation** with effective pip/uv operation and destination evidence, so UpgradePilot may establish the **exact proposed dependency requirement is satisfied in the exact relevant Python package environment at a justified observation boundary**. Not an inventory of all machine environments, a generic runner emulator, or a flag-only patch. The coherent architecture and Build authorization remain pending.
- **Active working memory:** `working-memory/2026-09-24_effective-package-manager-semantics-system-design.md`. Its **Naming and retrieval convention** defines readable names for historical short labels and candidate proof routes. Supporting learning clarification: `working-memory/2026-09-24_r2_state-witness-scope-checkpoint.md`; supporting **Package-State Proof Boundary Review**: `working-memory/2026-09-24_effective-semantics_state-proof-boundary-review.md`; supporting **Python Environment Identity** investigations: `working-memory/2026-09-24_python-environment-identity-evidence-investigation.md` and `working-memory/2026-09-24_python-environment-identity_supported-boundary-review.md`.
- **One compact active decision index:** `working-memory/2026-09-24_effective-package-manager-semantics-decision-register.md` now owns the retrievable purpose/onboarding checkpoint, the **agreed claim-relative environment-identity proof principle**, remaining concrete scope decisions, their status and their source links, and the finite **R2 closure map + depth/rabbit-hole stop rule**. The closure map is a non-quota zoom-out guide: investigate further only when a detail can materially change proof correctness, supported boundary, ownership/architecture, or decision-critical product capability; otherwise preserve it as unsupported/unresolved/deferred. Use this index for design selection; working-memory records preserve detailed progression, and controlling plans/specifications own promoted architecture/implementation decisions.
- **Agreed sequencing:** Preserve the existing explained-abstention maintainer-action evaluator. Defer its expansion or redesign while trustworthy evidence is developed; continue action-relative checks on whether evidence is actually decision-critical. Revisit action categories through their product/semantic owners when representative evidence justifies review. This does not mean the evidence system must be complete without limit before any action work.

**Next selected responsibility:** Continue **Effective Package-Manager Semantics — Supported Subsystem Boundary Design** at the active R2 checkpoint: **bounded virtual-environment activation → later bare `pip`**. Real public workflows show that the current first-sequential-command-only runtime-strengthening rule is too narrow for a normal supported venv case, so the boundary now admits a **bounded sequential reachability** direction: later-command support may advance only through predecessors whose successful semantics positively establish same-shell fall-through, preservation of required execution assumptions, and explicit material effects. Do not infer that every command in a successful step executed. The immediate task is to close the positive/negative premises for `python -m venv <env>` + standard generated activation + later bare `pip`, including `--without-pip`, path matching, same-step/separate-step evidence, and closer PATH mutation. Exact types and Build remain unauthorized.

**Subsequent responsibility:** Review the two independent package-state proof-source candidates and settle the coherent supported boundary; **Evidence Source, Type, and Data-Flow Design** (historical R3) follows only then. Reconcile `plans/RUNTIME_DEPENDENCY_STATE_PROOF_COMPLETION_PLAN.md` before implementation if the selected design materially changes that plan's sequence. **No product Build is authorized by this memory update.**

**Communication and record naming:** Write expressive topic/phase/proof titles first, with short historical identifiers only in parentheses for navigation. Explain unfamiliar terms at first use, and tie each engineering detail back to the product claim and current source responsibilities. Do not silently relabel older files or replace canonical specification, architecture, plan, and implementation owners with working-memory summaries.

For historical detail not repeated here, the previous complete live-state record is preserved in Git history at `0d3f32396288a4572b4b2632d0ddc1e2e9af78ee:MEMORY.md`, and all dated working-memory/plan/specification owners remain in place. Do not treat the dated records as competing live-state owners.
