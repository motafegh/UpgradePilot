# UpgradePilot Current Memory

**Last updated:** 2026-09-10
**Authority:** sole owner of the live project position, current blockers, selected continuation, and current learning depth.

## Live position

- **Current responsibility:** re-anchor overall synthesis design against the existing product-simulation evidence corpus, reconcile those distinctions with actual `PublicPullRequestInvestigation` producer reachability, then pressure action permission over the resulting heterogeneous state.
- **Mode:** Planning/Design + Learning-by-Doing. Source/test implementation is not yet admitted because stable synthesis semantics are still unresolved.
- **Selected plan:** `plans/OVERALL_EVIDENCE_SUFFICIENCY_AND_MAINTAINER_ACTION_SYNTHESIS_PLAN.md`.
- **Active working memory:** `working-memory/2026-09-10_synthesis-real-case-transfer-and-handoff.md`.
- **Previous synthesis working memory:** `working-memory/2026-09-08_overall-evidence-sufficiency-synthesis-orientation.md`.
- **Parent plan:** `plans/B2_IMPACT_APPLICABILITY_INVESTIGATION_FOUNDATION_PLAN.md`.
- **Completed prior plan:** `plans/ARTIFACT_SERVICEABILITY_PUBLIC_INVESTIGATION_INTEGRATION_PLAN.md`.
- **Latest closed integration proof record:** `working-memory/2026-09-08_artifact-serviceability-integration-proof.md`.
- **Framework status:** bounded ordinary-Python / LangGraph / LangChain investigation remains closed for now; no framework re-entry is justified by the synthesis responsibility alone.

## Current checkpoint

The artifact-serviceability public-investigation integration responsibility remains closed through the full canonical Learning-by-Doing cycle with deterministic WSL proof.

The synthesis plan was further refined on September 10 at commit `9472d392d3fafede6acb1fb25feef83470633e24` after Ali challenged the use of an unlabeled synthetic reasoning example and redirected the design toward the existing product-simulation arm. The plan now requires real/real-derived product-simulation transfer pressure before new hypothetical state families or new simulation work.

The existing product-simulation corpus already contains materially relevant evidence for the current responsibility:

- S007 for stale/redundant selected investigation and revalidation;
- S008 for artifact-serviceability, source fallback and stopping distinctions;
- S009 for repository context separate from technical applicability;
- S010 for multiple mechanisms, different target handling, context and bounded discovery coverage;
- S011 for a settled environment/coverage question with deeper compatibility unresolved;
- S012 for historical artifact provenance and a specific useful outside capability;
- Conversation-C investigation-selection pressure for distinct stop reasons;
- cross-candidate/context synthesis pressure for preserving an evidence vector before action projection;
- B2/X1 no-tool transfer evidence for question-relative stop/defer/unresolved distinctions.

These are discovery/pressure evidence, not current semantic authority. Historical maintainer-action labels are not imported as product rules.

The latest learner reasoning checkpoint also established the correct interpretation of the Python declaration selector example:

```text
post-attempt selector returns None
!= all worthwhile UpgradePilot investigation is finished
```

The specific selector may decline to repeat its supported exact-declaration acquisition while broader continuation remains unknown. Whether synthesis needs richer handoff state does not by itself justify redesigning the selector API; first reuse or derive existing producer facts and identify only genuine missing responsibility.

Action-relative sufficiency, favorable output as return to ordinary review, and the investigate/block distinction remain design findings to pressure and accept. The Charter's five outcome families remain unchanged. The Product Decision Model still leaves overall synthesis semantics open; specification acceptance is required before implementation.

## Latest material verification and restrictions

At `da8c4223d08ec6b4f8aed651fc5306da25c64654`, the September 10 review ran 53 focused tests successfully and reran all three existing offline correctness diagnostics. Command-text false positives, PR patch/revision mismatch, and workflow-attempt mixing still reproduced. These are controlled synthetic findings, not live incidents, prevalence estimates, fixes, or overall recommendation proof.

Subsequent commits through the current planning/working-memory re-anchor changed documentation/planning state only; they do not supersede that product verification or establish product correctness for synthesis.

Any action whose evidence depends on an affected producer needs a proven correction, enforceable restriction, or withheld permission; this applies to cautious recommendations as well as favorable ones. Independently justified evidence may remain usable, but independence must be traced.

## Durable implementation state entering synthesis design

The normal application path currently exposes at least:

- PR/revision/dependency identity fields, subject to the reproduced patch/revision correspondence limitation;
- dependency supported/problem state;
- exact-head workflow/CI coverage state;
- package and upstream evidence/problem states;
- Python-support candidate/applicability/investigation/relevance state;
- artifact-serviceability candidate/problem/no-candidate state;
- selected Target artifact-environment evidence/problems;
- artifact applicability that may correctly remain unresolved when exact target wheel compatibility is unavailable.

No top-level source owner currently converts that heterogeneous state into overall evidence sufficiency or one Charter maintainer action.

## Stable synthesis boundary recovered

The Product Decision Model explicitly separates:

```text
INVESTIGATION
Should UpgradePilot acquire more evidence, what next check is worth pursuing,
and has worthwhile investigation stopped?

LATER SYNTHESIS / POLICY
Given all candidates, repository context, observations, failures, and remaining uncertainty,
is the overall evidence state sufficient for a maintainer-facing output and how do policy/
residual-risk considerations affect that output?
```

The current work owns the second question only at a newly admitted bounded B2 level. Product-simulation evidence may pressure this design but cannot redefine the accepted boundary itself.

## Current unresolved design questions

Before source implementation, resolve:

1. which current synthesis questions are already answered or materially pressured by existing real/real-derived simulation evidence, and which genuine semantic evidence gaps remain;
2. the producer-grounded handoff for attempted investigation, resulting knowledge/problem, and justified or unknown continuation;
3. which transferred distinctions are already representable or derivable by current producers versus genuinely missing or unreachable;
4. the minimum action-relative result and whether separate sufficiency state adds independent meaning;
5. action/sub-disposition/reason/check permissions, competing concerns, and the first supportable action subset;
6. necessary consequence/context facts, favorable bounded coverage, and any future-condition or outside-responsibility basis needed for defer;
7. affected-evidence restrictions and reachable typed failures versus unsupported/operational outputs;
8. the correct stable specification owner and acceptance of the coherent decision matrix before implementation.

## Simplest credible baseline

The selected design baseline remains deterministic transparent composition over owned typed state:

```text
owned evidence/result state
→ explicit sufficiency/action conditions
→ bounded Charter action or abstention
→ decisive reasons + unresolved limits/checks
```

Do not begin from an LLM, graph, policy engine, numeric risk score, or agent planner.

## Immediate continuation

Begin the selected plan's **real-case evidence-transfer** step rather than extending the previous synthetic Python example.

First bounded contrast:

```text
S007 + Conversation-C investigation-selection evidence
        ↕
current Python declaration selection / acquisition / result path
        ↓
map which investigation-continuation and stopping distinctions are:
available
→ derivable
→ genuinely missing
→ unreachable by current normal producers
```

This should answer what synthesis can know about selected/attempted investigation and justified continuation without prematurely adding a new selector return type or generic stop model.

Then repeat the same transfer/reachability analysis with **S008 + current artifact-serviceability / Target artifact-environment state**, keeping wheel loss, exact target compatibility, source fallback availability, source fallback success and relevant environment/CI coverage separate.

Preserve the result as a compact evidence-transfer + producer-reachability map in the active working memory. Only after these real contrasts are grounded should the decision matrix resume action-permission pressure.

If a semantic question remains genuinely uncovered after relevant S001–S012 and pressure evaluations are checked, specify the exact evidence gap before asking the product-simulation arm for new work. Do not create S013 merely for breadth.

## Active engineering constraints

- Mechanism-specific truth remains owned by mechanism/domain modules; synthesis consumes rather than rewrites it.
- Product-simulation cases are discovery/pressure evidence, not semantic or implementation authority.
- Real public, real-derived, current source/test, and synthetic evidence classes must remain distinguishable.
- Synthetic convenience must not replace adequate existing real-case pressure.
- Missing evidence is not negative evidence.
- Technical applicability is not equivalent to overall upgrade safety or maintainer action.
- Investigation stopping is not overall evidence sufficiency.
- Repository context must not be overloaded into one technical candidate object.
- Residual uncertainty must remain explicit and may justify different outputs only under accepted semantics.
- Static configuration evidence remains distinct from runtime execution evidence.
- Overall synthesis must not manufacture certainty from heterogeneous partial evidence.
- Candidate-discovery incompleteness must not silently become “no other impact exists.”
- Existing exact repository/revision/dependency/provenance identity must survive aggregation.
- No objective-safety claim, automatic merge, approval, commenting, or target mutation.
- Do not automatically add more mechanisms, new simulation cases, or restart framework work merely because synthesis sees unresolved evidence.

`UP-SKILL:upgradepilot-learning-by-doing`  
`UP-SKILL:upgradepilot-planning-design`  
`UP-SKILL:upgradepilot-working-memory`
